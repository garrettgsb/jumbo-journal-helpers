<h1><%= @user.name %></h1>
<h2>Journals</h2>
<% if @journals.length > 0 %>
  <% @journals.each do |journal| %>
    <a class="card"><%= journal.title %> (<%= journal.entries.length %> entries)</a>
  <% end %>
<% else %>
  <p>This user has no journals 😮</p>
<% end %>
